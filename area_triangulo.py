"""
Esse algoritimo calcula a área do triangulo com a formula at = b*a/2


"""

base = input("Entre com o valor da base, em METROS")
altura = input("Entre com valor da altura em METROS")

base = float(base)
altura = float(altura)

area = (base*altura)/2

print("A area do triangulo é: ", area)

